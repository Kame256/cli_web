doctype html
html(lang="ja")
  head
    meta(charset="utf-8")
    link(rel="stylesheet" href="../css/contact_a.css")
    link(rel="stylesheet" href="../css/parallel.css")
    title contact
  body
   include nav
   br
   .contact
    .container
     form(action="anko" method="post") 
      .item1
       p お名前(必須) 
       input(required type="text" name="name")
       p 企業または大学(自由記入)
       input( type="text" name="form")
       p メールアドレス(必須)
       input(required type="text" name="mail" plceholder="anlo@inlo.com" pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,3}$")
       br
      .item2
       p お問い合わせの種類: 
       select(name="select_type")
        option(value="opinion")
         |ご意見
        option(value="failure")
         |不具合
        option(value="hope")
         |願望,相談
       p 内容
       textarea(type="message" name="message")
       br
       input(type="submit")
       
        
